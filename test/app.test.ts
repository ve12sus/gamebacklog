import { expect } from 'chai'

describe('Gamebacklog app test suite', function() {
  describe('#newFunction()', function() {
    it('test for this thing', function() {
      expect(newFunction(data)).to.deep.equal([]);
    });
  });
});
